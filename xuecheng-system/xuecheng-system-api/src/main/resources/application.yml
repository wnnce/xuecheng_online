server:
  servlet:
    context-path: /system
  port: 8040
spring:
  profiles:
    active: service
logging:
  pattern:
    dateformat: 'yyyy-MM-dd HH:mm:ss-SSS'
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  packages-to-scan: com.zeroxn.xuecheng.content